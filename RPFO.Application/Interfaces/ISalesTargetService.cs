
using RPFO.Application.Helpers;
using RPFO.Data.Entities;
using RPFO.Data.ViewModel;
using RPFO.Utilities.Dtos;
using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;

namespace RPFO.Application.Interfaces
{
    public interface ISalesTargetService
    {
        //Task<GenericResult> GetAll(string CompanyCode, string SetupType,  string Id, DateTime? FromDate, DateTime? ToDate); 
        ////Task<GenericResult> GetByCode(string CompanyCode, string Code);  
        //Task<GenericResult> Create(MSalesTarget model);
        //Task<GenericResult> Update(MSalesTarget model);
        //Task<GenericResult> Delete(MSalesTarget model);
        //Task<GenericResult> Import(DataImport model);
    }
}
